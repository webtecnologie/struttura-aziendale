version: 1
metadata:
  name: filesystem
  description: Accesso ai file locali del progetto
servers:
  - type: filesystem
    id: filesystem
    config:
      roots:
        - path: "C:\\Users\\alessandro.petrone\\Documents\\Struttura-Aziendale"